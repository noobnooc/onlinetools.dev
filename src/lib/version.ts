/**
 * Single source of truth for the release stamp. Versions are dates:
 * bump on every deploy — the footer badge and the sitemap <lastmod>
 * both read from here.
 */
export const SITE_VERSION = '2026.07.21';
export const SITE_UPDATED = '2026-07-21';
